---
description: Turn the TokenLens estimate gate on or off, or show its status
argument-hint: "[on|off|status]"
allowed-tools: Bash(node:*)
---

!`node "${CLAUDE_PLUGIN_ROOT}/scripts/control.mjs" "$ARGUMENTS"`

Show the output above to the user exactly as written, then stop. Do not run any
other tool and do not add commentary.
